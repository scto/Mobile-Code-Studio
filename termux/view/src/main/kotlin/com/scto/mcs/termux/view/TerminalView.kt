package com.scto.mcs.termux.view

import android.content.Context
import android.util.AttributeSet
import android.view.View

/**
 * Stub für die TerminalView, um Kompilierungsfehler in TerminalScreen zu beheben.
 */
class TerminalView @JvmOverloads constructor(
    context: Context,
    attrs: AttributeSet? = null,
    defStyleAttr: Int = 0
) : View(context, attrs, defStyleAttr) {
    // Hier wird die eigentliche Terminal-Emulations-Logik implementiert
}
